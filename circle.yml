machine:
  node:
    version: 4.3.0

test:
  override:
    - node_modules/.bin/mocha test --reporter mocha-junit-reporter --recursive:
        environment:
          MOCHA_FILE: $CIRCLE_TEST_REPORTS/junit/test-results.xml